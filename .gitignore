# R
.Rhistory
.RData
.Rproj.user/
*.Rproj
renv/library/
renv/python/
renv/staging/
renv/local/

# Quarto
docs/_site/
docs/.quarto/
docs/_freeze/
docs/_generated/
*.quarto_ipynb

# Generated artefacts (CSV/RDS) — keep raw_records and screening_log under
# version control as audit trail; ignore everything else under data/ that's
# auto-derived.
docs/figures/forest_*.png
docs/figures/funnel_*.png

# OS
.DS_Store
Thumbs.db

# Full-text PDFs are kept locally during a workflow run but not committed
# (often paywalled or large). Audit log + extracted JSON live in CSVs.
data/fulltext/*.pdf
data/fulltext/*.xml
!data/fulltext/.gitkeep
